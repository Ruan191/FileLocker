using Xunit;
//using File_Locker.Services;
//using File_Locker.ViewModels;
using Moq;

namespace FileLocker.Tests
{
    //public class UnitTest1
    //{
    //    [Fact]
    //    public void CounterText_Updates_When_CounterBtn_Executed()
    //    {
    //        var mockCounter = new Mock<ICounter>();
    //        mockCounter.SetupSequence(c => c.Count)
    //            .Returns(0)
    //            .Returns(1);

    //        mockCounter.Setup(c => c.CountUp()).Callback(() => { });

    //        var vm = new MainPageViewModel(mockCounter.Object);

    //        vm.OnCounterButtonClicked.Invoke();

    //        Assert.Equal("Clicked 1 times", vm.CounterText);
    //    }
    //}
}
